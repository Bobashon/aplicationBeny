<% provide(:title,"Usuarios")%>
<h1>Todos los Usuarios</h1>

<%= will_paginate @users %>

<ul class="users">
  <% @users.each do |user| %>
     <%= render user %>
  <% end %>
</ul>

<%= will_paginate @users %>