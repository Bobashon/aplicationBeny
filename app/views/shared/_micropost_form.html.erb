<%= form_for(@micropost) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.text_area :content, placeholder: "Crea un nuevo Post..." %>
    </div>
    <%= f.submit "Publicar", class: "btn btn-large btn-primary" %>
<% end %>